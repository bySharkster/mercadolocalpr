/**
 * Abstract base class representing a command in the application.
 * Commands encapsulate a specific action or intent to be executed.
 * Concrete command classes should extend this class and provide their own implementation.
 */
export default abstract class Command {}
